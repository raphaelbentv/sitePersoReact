{
  "name": "venio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ionos": "vite build && node scripts/create-htaccess.js",
    "preview": "vite preview",
    "postbuild": "node scripts/create-htaccess.js && node scripts/generate-sitemap.js",
    "sitemap": "node scripts/generate-sitemap.js",
    "deploy:check": "node scripts/prepare-deploy.js",
    "deploy:ionos": "npm run build:ionos && npm run deploy:check"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet-async": "^2.0.5",
    "react-router-dom": "^7.9.6"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  }
}
